{
  "name": "@phantomtrace/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PhantomVerum — Cliente (local, privado) + Explorer (público) + Sistema visual",
  "scripts": {
    "dev": "npm run build:shared && concurrently -n cliente,explorer,sistema -c magenta,cyan,gray \"npm:dev:cliente\" \"npm:dev:explorer\" \"npm:dev:sistema\"",
    "dev:cliente": "vite --config vite.cliente.config.ts",
    "dev:explorer": "vite --config vite.explorer.config.ts",
    "dev:sistema": "vite --config vite.sistema.config.ts",
    "build": "npm run build:shared && tsc --noEmit && npm run build:cliente && npm run build:explorer && npm run build:sistema",
    "build:cliente": "vite build --config vite.cliente.config.ts",
    "build:explorer": "vite build --config vite.explorer.config.ts",
    "build:sistema": "vite build --config vite.sistema.config.ts",
    "build:shared": "npm run build --workspace=@phantomtrace/shared",
    "test": "npm run build:shared && vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.1.0",
    "@fontsource/space-grotesk": "^5.1.0",
    "@midnight-ntwrk/compact-runtime": "0.16.0",
    "@phantomtrace/shared": "0.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "concurrently": "^9.1.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.6.0",
    "vitest": "^4.1.10"
  }
}
