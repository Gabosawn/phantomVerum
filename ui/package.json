{
  "name": "@phantomtrace/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PhantomVerum — Cliente (local, privado) + Explorer (público) + Sistema visual",
  "scripts": {
    "dev": "concurrently -n cliente,explorer,sistema -c magenta,cyan,gray \"npm:dev:cliente\" \"npm:dev:explorer\" \"npm:dev:sistema\"",
    "dev:cliente": "vite --config vite.cliente.config.ts",
    "dev:explorer": "vite --config vite.explorer.config.ts",
    "dev:sistema": "vite --config vite.sistema.config.ts",
    "build": "tsc --noEmit && npm run build:cliente && npm run build:explorer && npm run build:sistema",
    "build:cliente": "vite build --config vite.cliente.config.ts",
    "build:explorer": "vite build --config vite.explorer.config.ts",
    "build:sistema": "vite build --config vite.sistema.config.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.1.0",
    "@fontsource/space-grotesk": "^5.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "concurrently": "^9.1.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0"
  }
}
