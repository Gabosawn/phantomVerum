# contracts/ — Contratos Compact

**Dueño: JUAN** · Tasks: T1 (backup), T2 (`registrarOrganizacion` + `denunciar`), T3 (`revelarAutoria`)

Spec semántica: `../docs/01-arquitectura.md` — leer §8 antes de escribir una
línea (adaptar sintaxis a la versión instalada, nunca la semántica; desvíos a
`../docs/DESVIOS.md`).

> ⚠️ **Vacío a propósito.** El reglamento exige código 100 % nuevo desde el
> viernes 7/8 a las 10:00. Ni una línea antes.
